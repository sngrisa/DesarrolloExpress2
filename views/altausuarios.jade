extends ./layout.jade
block contenido
    <div class="container content text-center">
    form(action="/users", method="POST")
        div(class="form-group")
        <img src="https://thumbs.gfycat.com/BouncyLoneBlackwidowspider-small.gif">
        br
        h2 Dar de alta a los usuarios
        br
        p Complete los siguientes espacios para dar de alta a un usuario en el sistema: 
        br
        label(for="nombre") Nombre:
        br
        input(type="text" placeholder="Ingrese el nombre" name="nombre" id="nombre" class="form-control" pattern="^[A-Za-zÑñÁáÉéÍíÓóÚúÜü\s]+$" required)
        br
        label(for="email") Email:
        br
        input(type="email" placeholder="Ingrese el correo" name="email" id="email" class="form-control" pattern="^[a-z0-9]+(\.[_a-z0-9]+)*@[a-z0-9-]+(\.[a-z0-9-]+)*(\.[a-z]{2,15})$" required)
        br
        br
        label(for="telefono") Numero de telefono:
        br
        input(type="tel" placeholder="Ingrese su numero telefonico" name="telefono" id="telefono" class="form-control" pattern="^[0-9]+$" required)
        br
        label(for="password") Contraseña:
        br
        input(type="password" placeholder="Ingrese la contraseña" name="password" id="password" class="form-control" min=4 max=15 required)
        br
        label(for="password_confirmation") Confirmar Contraseña:
        br
        input(type="password" placeholder="Ingrese nuevamente la contraseña" name="password_confirmation" id="password_confirmation" class="form-control" min=4 max=15 required)
        br
        br
        br
        input(class="btn btn-info" type="submit" value="Enviar Datos")
        br
        br

        